---
import ProductCardV1 from "./product-cards/ProductCardV1.astro";
// import ProductCardV2 from "./product-cards/ProductCardV2.astro";

interface Props {
  product: any;
  variant?: 'v1' | 'v2';
}

const { product, variant = 'v1' } = Astro.props;
---

{variant === 'v1' && <ProductCardV1 product={product} />}
<!-- {variant === 'v2' && <ProductCardV2 product={product} />} -->